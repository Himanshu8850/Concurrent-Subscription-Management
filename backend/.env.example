# Server Configuration
NODE_ENV=development
PORT=3000

# Database
MONGO_URI=mongodb://localhost:27017/cstl-stripe-clone

# Redis (optional - for distributed locks and job queues)
REDIS_URL=redis://localhost:6379

# Authentication (if implementing JWT-based auth)
JWT_SECRET=your-secret-key-change-in-production

# Payment Provider (if integrating Stripe)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Application Settings
RESERVATION_POLICY=charge-first
IDEMPOTENCY_TTL_DAYS=30

# Logging
LOG_LEVEL=info
